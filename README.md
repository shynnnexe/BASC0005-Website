
BASC0005: Website Project

  This is a code bundle for Website Project - Mount Everest (Team Grunge). 

  ## Running the code
  Run `npm i` to install the dependencies.
  Run `npm run dev` to start the development server.
  
